{
  "name": "eveagents-railway-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "prepare:agent": "node --env-file-if-exists=.env scripts/prepare-agent.mjs",
    "build": "npm run prepare:agent && eve build",
    "start": "node scripts/start.mjs",
    "test": "node --test scripts/prepare-agent.test.mjs"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.19",
    "@ai-sdk/google": "^4.0.23",
    "@ai-sdk/openai": "^4.0.20",
    "@vercel/connect": "0.4.0",
    "ai": "^7.0.26",
    "eve": "0.25.2",
    "just-bash": "3.0.1",
    "zod": "4.4.3"
  }
}
